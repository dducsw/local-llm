# ==============================================================================
# vLLMlocal — Application Environment Configuration Template
# ==============================================================================
# Copy this file to .env (inside app/.env or at project root) and customize values.

# ------------------------------------------------------------------------------
# 1. Administrator Dashboard Credentials
# ------------------------------------------------------------------------------
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_TOKEN=admin123

# ------------------------------------------------------------------------------
# 2. Database and Model Registry Paths
# ------------------------------------------------------------------------------
# When running from inside app directory:
MODEL_CONFIG=./config/models.json
KEY_DB=./data/gateway.db

# ------------------------------------------------------------------------------
# 3. Network and Server Settings
# ------------------------------------------------------------------------------
UPSTREAM_TIMEOUT=3600
LOG_LEVEL=INFO

# ------------------------------------------------------------------------------
# 4. Optional: Remote HPC Slurm Supervision via SSH (when gateway runs on PC/VM)
# ------------------------------------------------------------------------------
# HPC_SSH_HOST=node16.gitc.hpc
# HPC_SSH_USER=ducledinh
# HPC_SSH_KEY=~/.ssh/id_rsa
# HPC_REMOTE_DIR=~/local-llm/infra
